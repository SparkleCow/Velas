package com.sparklecow.velas.entities.ingredient;

public record IngredientResponseDto(Ingredients name,
                                    Double amount,
                                    Double price){
}
